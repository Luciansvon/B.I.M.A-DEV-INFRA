paths:
  .github/workflows/trusted-repository-audit.yml:
    ignore:
      # GitHub.com exposes these reusable-workflow identity properties, but
      # actionlint 1.7.12 has not added them to its job-context schema yet.
      - 'property "workflow_(repository|ref|sha)" is not defined in object type'
