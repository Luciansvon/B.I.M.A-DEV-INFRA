name: Trusted launcher proof

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trusted-repository-audit:
    name: Trusted repository audit proof
    permissions:
      contents: read
    uses: Luciansvon/B.I.M.A-DEV-INFRA/.github/workflows/trusted-repository-audit.yml@1103ac3978441253251a9a818a4977fbdbdb350a # trusted-launcher-v1
    with:
      artifact-name: trusted-launcher-proof
